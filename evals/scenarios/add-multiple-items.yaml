name: "Add multiple items to cart"
description: "Test if the agent can add multiple products to cart sequentially"
url: "/shop.html"
task: "Add the Smart Watch and USB-C Hub to the cart"
timeout: 60000

assertions:
  - type: selector
    selector: "#cartCount"
    expected: "2"
  
  - type: localStorage
    key: "cartCount"
    expected: "2"
  
  - type: tool_called
    tool: "findAndClick"



